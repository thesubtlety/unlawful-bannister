name: vulnerable-action
run-name: ${{ github.actor }} is checking the date
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: cat /proc/*/environ
        env:
                secret_test: ${{secrets.TEST_TOKEN}}
